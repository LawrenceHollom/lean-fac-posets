import AharoniKorman.Completion.ChainEquivalence

/-! Construction of the glued quotient type `H(P)` and its principal/nonprincipal APIs. -/

namespace AharoniKorman.Completion

-- Downstream modules must use the API built here rather than unfold the nested quotients.

end AharoniKorman.Completion
